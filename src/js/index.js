import { router } from "./routes/routes.mjs";
/**
 * Invokes the router function to handle page routing based on the current URL path.
 *
 * This will check the current page URL and trigger the corresponding route handler functions
 * for the main pages like home, login, registration, admin profile, listings, and other pages.
 *
 * @function
 * @returns {void}
 */
router();